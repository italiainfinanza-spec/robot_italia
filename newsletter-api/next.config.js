/** @type {import('next').NextConfig} */
const nextConfig = {
  // Server-side rendering enabled (required for API routes)
  // No output: 'export' - this is an API service
}

module.exports = nextConfig